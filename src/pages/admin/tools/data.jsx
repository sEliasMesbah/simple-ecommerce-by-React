export default function useData(){
    const saveData = (component) =>{
        let formData = {}

        console.log(component.state.refs);

        
        
    }

    return {
        saveData
    }
}